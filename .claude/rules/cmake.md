## CMake ビルドルール

CMake を実行する際は、以下の手順に従うこと:

1. `CMakeLists.txt` のあるディレクトリで `gen` ディレクトリを作成する
2. `gen` ディレクトリに移動する
3. `cmake ..` を実行する

```bash
mkdir -p gen && cd gen && cmake ..
```

- ソースディレクトリで直接 `cmake .` を実行しないこと
- ビルド成果物は常に `gen/` 以下に生成すること
- `cmake ..` するときにオプションを付与するのはよい
